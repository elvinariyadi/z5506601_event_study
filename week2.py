variable = [
    ('a',10),
    ('b',2)
]
var={key:value for key,value in variable}
print(var)

ticker_lst[10,12]

def verify_tickers(tic_exchange_dic, tickers_lst=None):
    if tickers_lst is not None:
        if not tickers_lst:
         raise Exception("Ticker is an empty list.")
        if not tickers_lst == var.get(value):
         raise Exception(f"'{ticker_lst}' is not a valid a key tic_exchange_dic.")